# Computer-vision
In this repository there are three transfer learning models based on MobileNetV3. Each model was trainned with two different datasets and compared with metrics such as trainning time, model size, number of parameters, Precision, recall and F1-score.
